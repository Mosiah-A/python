#calcular o aluguel de um carro
d = int(input('Quantos dias foram alugados? '))
km = float(input('Quantos Km foram rodados? '))
p = (d * 60) + (km * 0.15)
print(' O total apagar é R${:.2f}'.format(p))

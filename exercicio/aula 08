#from math import sqrt
#num = int(input('Digite um número: '))
#raiz = sqrt(num)
#print ("A rais de {} é {}".format(num,raiz))

import math
num = int(input('Digite um número: '))
raiz = math.sqrt(num)
#print('A raiz de {} é {}'.format(num,math.ceil(raiz)))
print('A raiz de {} é {:.2f}'.format(num,raiz))
